//második feladat 

/* function countToArray(firstNumber, lastNumber) {
    const start = firstNumber;
    const end = lastNumber-1;
    const arr = [...Array(end - start + 1).keys()].map(x => x + start);
    let stArr = arr.map(String);
    return stArr
  }
  
 console.log(countToArray(4,8)) */

 